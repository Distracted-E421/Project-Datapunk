# Set default behavior for line endings
eol=lf

# Ensure text files have consistent line endings
*.py text
*.yml text
*.yaml text
*.json text
*.md text
*.sh text

# Binary files should be treated as binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary

# Handle Dockerfile files properly
Dockerfile text

# Enforce LF endings for shell scripts
*.sh eol=lf

# Mark large data files to avoid diff
*.tar -diff
*.sql -diff